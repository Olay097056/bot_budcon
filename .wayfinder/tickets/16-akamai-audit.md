# Ticket 16 — Audit every TTM HTTP call for Akamai exposure

**Type**: research
**Status**: closed — resolved 2026-09-01
**Label**: `wayfinder:research`
**Depends**: none (frontier)

## Question

List every place in the codebase that hits a `thaiticketmajor.com` or `thaiticketmajor` subdomain (fetch, page.goto, wreq, undici, Playwright) — including `src/discover.ts`, `src/zones.ts`, `src/book.ts` (selectZone/fixed.php/pickSeats), `src/watch.ts`, `src/bot-engine.ts`, `src/cookies.ts`, `src/login.ts`, and the invisible bridge. For each call, note: URL pattern, transport (raw fetch vs wreq-js vs Playwright), headers/cookies sent, current fallback (none vs browser vs cache), and observed Akamai behavior (200 vs 403/407/429) from `44efd37` and ticket 09 recon. Outcome is a single table the next tickets can decide from — no code change in this ticket.

## Answer

Audited `src/` at `ebacbfa` (grep `fetch|goto|zones|fixed|concert` + manual read of `discover.ts`, `watch.ts`, `bot-engine.ts`, `book.ts`, `server.ts`, `login.ts`).

| # | File:Line | URL pattern | Transport | Headers / Cookies | Fallback | Observed Akamai | Verdict |
|---|-----------|-------------|-----------|-----------------|----------|-----------------|---------|
| 1 | `discover.ts:142 defaultFetcher` | `https://www.thaiticketmajor.com/concert/` + `zones.php?query=<q>` (loop) | `native fetch` + `ProxyAgent` if `BOT_BUDCON_PROXY` | `Cookie: buildCookieHeader` + `Referer: thaiticketmajor.com/` + `UA Firefox 128` | `fetchWithBrowserFallback` (403/429 → `fetchViaBrowser`) + `discover-cache.json` hydrate | `concert/` 403/407 on datacenter IP (GitHub), 200 on residential; `zones.php` 200 with cookies, 302→signin without | **HARDENED** — the only path that survives concert/ WAF today (44efd37) |
| 2 | `discover.ts:186 fetchViaBrowser` | same two URLs (called only on 403/429) | `Playwright BrowserContext.newPage().goto` via `BotEngine.getContext()` (reuses Firefox profile + `config.proxy`) | Full browser jar (`PHPSESSID`, `ttkname`, `_abck`, `bm_sv`, `bm_sz`, `ak_bmsc`) + TLS/JA3 of real Firefox | none needed (browser is the fallback) | 200 even when fetch is 403 (IP still WAF'd? then still 403 — hence cache layer) | **HARDENED** — but reuses one `BrowserContext` for whole discover call |
| 3 | `watch.ts:90 defaultFetcher` | `zones.php?query=<q>` (poll every 5s) | `native fetch` | `Cookie: buildCookieHeader` + `Accept + UA` only (no Referer) | **none** — on non-200 just `sleep(interval)` and retry next tick | Poll stalls on 403 until WAF lifts; no browser, no cache, no wreq | **VULNERABLE — frontier for ticket 17** |
| 4 | `watch-manager.ts` | delegates to `watch.ts` fetcher (injected `fetcher?` or default) | inherits #3 | same | same | same | **VULNERABLE (inherits #3)** |
| 5 | `server.ts:401 /api/events/preview` | `zones.php?query=<q>` (single probe) | `native fetch` | `Cookie + Referer + UA` (mirrors discover) | `cache/discover-cache.json` hydrate if `waf-verify\|Access Denied\|403` or zones empty | Live 403 → serves cached zones if hit exists, else `zones:[]` | **PARTIAL** — no browser fallback (unlike discover), depends on cache freshness |
| 6 | `bot-engine.ts:92 fetchViaWreq` | any TTM URL (intended for zone polls) | `wreq-js` (Chrome 149 JA3/JA4, HTTP/2) | caller-supplied headers (no auto cookie) | none | 200 OK on `thaiticketmajor.com/` per ticket 02 smoke, but **not wired** to discover/watch | **AVAILABLE BUT UNUSED** |
| 7 | `book.ts:85 page.goto` | `zones.php?query=<q>` | `Playwright page.goto` (via `engine.getContext()`) | Full browser jar + `config.proxy` | none needed | 200 — browser bypasses Akamai edgesuite | **HARDENED** |
| 8 | `book.ts:241 zoneUrl → fixed.php/festival.php` | `fixed.php?k=&zone=&round=` / `festival.php?...` | `page.evaluate(selectzone)` → `location.href` (sends `Referer: zones.php?rdId=&k=&tk=`) | Browser jar | retry via `location.href = fullUrl` if selectzone missed | Before Referer fix: `errcode=9`; after `297693f`: 200 63k/170k + tableseats | **HARDENED** (Referer + round_change jQuery trigger fixed in `a213578`) |
| 9 | `book.ts: pickSeats/confirmSeats/payment/finalConfirm` | `#tableseats`, `#booknow` clicks | `Playwright page.$ / click / $$eval` | browser | none | N/A (DOM) | **HARDENED** |
| 10 | `login.ts + invisible_browser.py` | `https://www.thaiticketmajor.com/user/signin.php` | `Playwright persistent Firefox` (C++ patched, `--window-position=1100,40`) + `BOT_BUDCON_HEADLESS=0` | Real browser TLS + `_abck` generated by Akamai sensor | poll 2s/5min for `PHPSESSID + ttkname`, persists to `cookies.json` | 200 — never blocked (patch bypasses detection) | **HARDENED** |
| 11 | `cookies.ts` | no HTTP | helper `buildCookieHeader(host)` — filters by `domain` (apex or strict subdomain), `expires` | — | — | — | **SUPPORT** |

**Summary counts**: HARDENED 6 (1,2,7,8,9,10), PARTIAL 1 (5), VULNERABLE 2 (3,4), UNUSED 1 (6)

**Decision for next tickets**:
- Ticket 17 must unify #3/#4/#5 onto the hardened path of #1/#2 (and consider #6 wreq-js vs browser trade-off). No new raw `fetch` to TTM without `fetchWithBrowserFallback + cache`.
- Ticket 18 owns the cache contract (#1 and #5 share `discover-cache.json` but with different `loadCache` impls — unify).
- Watch's comment "We avoid wreq-js here on purpose" is stale — wreq-js exists for exactly this case and should be evaluated against browser cost.

**Artifacts**: grep outputs at audit time (`grep -rn "fetch\|goto\|zones\|fixed"` + reads of `discover.ts:142-291`, `watch.ts:90-103`, `server.ts:379-442`, `book.ts:85-291`, `bot-engine.ts:92-180`).
